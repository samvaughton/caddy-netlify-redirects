{
	admin off
	auto_https off
	order netlify_redirects before redir
}

:2021 {
	log
    encode gzip
	root * /srv
	file_server

	netlify_redirects {
		/about-us /contact
		/url/too/long/* /shortened/:splat
	}
}
